# Base URL of the backend API (server/), including the /api/v1 prefix.
# Inlined into the client bundle at build time (NEXT_PUBLIC_* vars are not
# read at runtime) — set this to the deployed backend's URL before running
# `next build` for production.
NEXT_PUBLIC_API_URL=http://localhost:3030/api/v1
